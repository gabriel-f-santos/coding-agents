# AGENTS.md — {{APP_NAME}} ({{STACK}})

Overrides the root AGENTS.md for this subtree (nearest-file-wins). Only the delta is here.

## Stack
- {{STACK}} {{VERSIONS}}. Dev port {{PORT}}.

## Commands (run from this dir; root `task {{ALIAS}}:*` proxies these)
- Install:     `{{INSTALL_CMD}}`
- Dev server:  `{{DEV_CMD}}`
- Lint:        `{{LINT_CMD}}`
- Format:      `{{FMT_CMD}}`
- Test all:    `{{TEST_CMD}}`
- Single test: `{{TEST_ONE_CMD}}`

## Code style
- {{FORMATTER}} is authoritative. {{STYLE_NOTES}}

## Testing rules
- {{TEST_NOTES}}

## Non-obvious / gotchas
- {{GOTCHAS}}
